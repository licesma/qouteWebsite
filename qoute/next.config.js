/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    appDir: true,
  },
};

module.exports = {
  images: {
    domains: ["firebasestorage.googleapis.com"],
  },
  experimental: {
    appDir: true,
  },
};
